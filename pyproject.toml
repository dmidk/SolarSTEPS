[project]
name = "solarsteps"
version = "0.1.0"
description = "solarsteps: A Python package for solar nowcasting using the STEPS algorithm"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.3",
    "properscoring>=0.1",
    "pysteps>=1.18.1",
    "scipy>=1.16.2",
]
